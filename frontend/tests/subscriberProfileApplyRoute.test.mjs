/**
 * CI-visible wrapper for Profile Apply route production tests.
 * Imports and exercises the real production handler with DI.
 * Runs under: npm test (tsx --test tests/*.test.mjs)
 */
import '../src/server/__tests__/subscriberProfileApplyRoute.test.ts';

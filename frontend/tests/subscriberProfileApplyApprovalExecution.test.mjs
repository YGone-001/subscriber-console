/**
 * CI-visible wrapper for Profile Apply approval executor tests.
 * Imports and exercises the real production executor with DI.
 * Runs under: npm test (tsx --test tests/*.test.mjs)
 */
import '../src/server/__tests__/subscriberProfileApplyExecutor.test.ts';
